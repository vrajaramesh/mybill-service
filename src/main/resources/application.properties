# ── Database (common) ──────────────────────────────────────────
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ── Database (cloud - overridden by k8s secrets in GKE) ────────
spring.datasource.url=jdbc:postgresql://34.170.148.94:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=Vrrvt50707

# ── File upload limits ─────────────────────────────────────────
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# ── Schema init (runs init-public-schema.sql on every startup) ─
# Safe to run repeatedly — all statements use IF NOT EXISTS
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:init-public-schema.sql
spring.sql.init.continue-on-error=true
# Ensure SQL init runs before Hibernate entity manager initializes
spring.jpa.defer-datasource-initialization=true

# ── Server port (reads Cloud Run's PORT env var) ────────────────
server.port=${PORT:8080}

# ── CORS ───────────────────────────────────────────────────────
app.cors.allowed-origins=*

# ── JWT ────────────────────────────────────────────────────────
app.jwt.secret=mybill-super-secret-key-change-this-in-production-min32chars
app.jwt.expiry-days=7

# ── Image generation provider: falaai (default) or openai ─────
# Switch by changing this one line — no code changes needed.
image.generation.provider=falaai

# ── Fal.ai (FLUX Dev — better fabric fidelity than OpenAI) ────
falaai.api.key=
falaai.api.url=https://fal.run

# ── OpenAI ─────────────────────────────────────────────────────
openai.api.key=
openai.vision.url=https://api.openai.com/v1/chat/completions
openai.image.url=https://api.openai.com/v1/images/generations
openai.image.edit.url=https://api.openai.com/v1/images/edits
openai.responses.url=https://api.openai.com/v1/responses
openai.embed.url=https://api.openai.com/v1/embeddings

# ── Gemini (used for vision analysis - free tier) ──────────────
gemini.api.key=
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
gemini.chat.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# ── Google Cloud Storage ───────────────────────────────────────
gcs.bucket-name=mybill-product-images
gcs.project-id=project-0708fb26-e204-4400-ae9

# ── Groq (chatbot — llama-3.3-70b-versatile, 14400 req/day free) ─
groq.api.key=
groq.model=llama-3.3-70b-versatile
groq.api.url=https://api.groq.com/openai/v1/chat/completions

# ── FashionCLIP local microservice (python fashionclip_service/main.py) ──
fashionclip.service.url=https://fashionclip-service-276371417285.us-central1.run.app

# ── Razorpay ───────────────────────────────────────────────────
razorpay.key_id=${RAZORPAY_KEY_ID:}
razorpay.key_secret=${RAZORPAY_KEY_SECRET:}

# ── WhatsApp (Meta Graph API) ──────────────────────────────────
whatsapp.api.url=https://graph.facebook.com/v18.0

# ── Anthropic Claude ───────────────────────────────────────────
anthropic.api.key=
anthropic.api.url=https://api.anthropic.com/v1/messages

# ── Hermes Marketing Automation ───────────────────────────────
# Set to false to disable all scheduled campaigns
hermes.enabled=true
# mybill-whatsapp Express API base URL (port 3001)
hermes.whatsapp.url=http://localhost:3001
# Owner's WhatsApp phone for daily executive report (e.g. 919876543210)
hermes.owner.phone=
